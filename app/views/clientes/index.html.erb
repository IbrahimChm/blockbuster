<h1>Clientes</h1>

<div class="row">
  <% @clientes.each do |cliente| %>
    <div class="col-md-4">
      <div class="card mb-4">
        <div class="card-body">
          <h5 class="card-title"><%= cliente.nombre %></h5>
          <div class="btn-group">
            <%= link_to 'Editar', edit_cliente_path(cliente), class: 'btn btn-primary' %>
            <%= link_to 'Eliminar', cliente_path(cliente), method: :delete, data: { confirm: '¿Estás seguro de eliminar este cliente?' }, class: 'btn btn-danger' %>
            <%= link_to 'Películas Arrendadas', peliculas_arrendadas_cliente_path(cliente), class: 'btn btn-info' %>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>

<p><%= link_to 'Agregar nuevo cliente', new_cliente_path, class: 'btn btn-success' %></p>




